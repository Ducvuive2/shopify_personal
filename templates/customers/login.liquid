<div class="container py-5 my-5">
    <div id="login-form" class="login-form">
        {% form 'customer_login' %}
            {% include 'form-error', form: form %}
            <h1 class="h3">Customer Login</h1>
            <label for="emailAddress">Email Address</label>
            <input type="email" name="customer[email]" class="form-control rounded-0 my-2" id="emailAddress">
            <label for="password">Password</label>
            <input type="password" name="customer[password]" class="form-control rounded-0 my-2" id="password">

            <input type="submit" value="Sign in" class="btn btn-primary btn-lg w-100 rounded-0">
        {% endform %}

        <a id="forgotPassword" class="text-center" style="cursor:pointer;">Forgot your password?</a>
    </div>
    <div id="forgot_password_form" class="forgot-password-form d-none">
        {% form 'recover_customer_password' %}
            {% include 'form-error', form: form %}

            <div class="col-12">
                <label for="resetPassword" class="form-label">Email Address</label>
                <input class="form-control" type="email" name="email" id="resetPassword">
            </div>

            <input type="submit" value="Reset Password" class="btn btn-secondary mt-2">
        {% endform %}
        <a id="Login" class="text-center" style="cursor:pointer;">Have account?</a>
    </div>
</div>
